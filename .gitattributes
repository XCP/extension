# Auto detect text files and perform LF normalization
* text=auto

# Preserve byte-identical vendored wallet-sdk contract hashes on every platform.
src/core/amount-contract/amounts.ts text eol=lf
src/core/amount-contract/amounts-v1.json text eol=lf
